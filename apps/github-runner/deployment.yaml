apiVersion: apps/v1
kind: Deployment
metadata:
  name: github-runner
  namespace: github-runner
spec:
  replicas: 1
  selector:
    matchLabels:
      app: github-runner
  template:
    metadata:
      labels:
        app: github-runner
    spec:
      initContainers:
      - name: buildkit-config
        image: busybox
        command:
        - sh
        - -c
        - |
          mkdir -p /buildkit-config
          cat > /buildkit-config/buildkitd.toml <<'TOML'
          [registry."harbor.192.168.0.240.nip.io"]
            ca=["/etc/ssl/homelab/homelab-ca.crt"]
          TOML
        volumeMounts:
        - name: buildkit-config
          mountPath: /buildkit-config
      containers:
      - name: runner
        image: myoung34/github-runner:latest
        env:
        - name: REPO_URL
          value: https://github.com/MiguelABFlores/miguelabf-devops-portfolio
        - name: RUNNER_NAME
          value: homelab-runner
        - name: RUNNER_WORKDIR
          value: /tmp/runner/work
        - name: RUNNER_GROUP
          value: default
        - name: RUNNER_LABELS
          value: self-hosted,homelab,linux,amd64
        - name: ACCESS_TOKEN
          valueFrom:
            secretKeyRef:
              name: github-runner-token
              key: token
        - name: RUNNER_SCOPE
          value: repo
        - name: DISABLE_AUTO_UPDATE
          value: "true"
        - name: DOCKER_HOST
          value: tcp://localhost:2375
        - name: BUILDKITD_FLAGS
          value: --config /buildkit-config/buildkitd.toml
        - name: NODE_EXTRA_CA_CERTS
          value: /etc/ssl/homelab/homelab-ca.crt
        resources:
          requests:
            cpu: 200m
            memory: 512Mi
          limits:
            cpu: 2000m
            memory: 3Gi
        volumeMounts:
        - name: work
          mountPath: /tmp/runner/work
        - name: buildkit-config
          mountPath: /buildkit-config
        - name: homelab-ca
          mountPath: /etc/ssl/homelab
          readOnly: true
      - name: dind
        image: docker:24-dind
        securityContext:
          privileged: true
        env:
        - name: DOCKER_TLS_CERTDIR
          value: ""
        command:
        - sh
        - -c
        - |
          mkdir -p /etc/docker/certs.d/harbor.192.168.0.240.nip.io
          cp /homelab-ca/homelab-ca.crt \
             /etc/docker/certs.d/harbor.192.168.0.240.nip.io/ca.crt
          dockerd-entrypoint.sh
        resources:
          requests:
            cpu: 200m
            memory: 512Mi
          limits:
            cpu: 2000m
            memory: 2Gi
        volumeMounts:
        - name: work
          mountPath: /tmp/runner/work
        - name: docker-storage
          mountPath: /var/lib/docker
        - name: buildkit-config
          mountPath: /buildkit-config
        - name: homelab-ca
          mountPath: /homelab-ca
          readOnly: true
      volumes:
      - name: work
        emptyDir: {}
      - name: docker-storage
        emptyDir: {}
      - name: buildkit-config
        emptyDir: {}
      - name: homelab-ca
        secret:
          secretName: homelab-ca-cert
